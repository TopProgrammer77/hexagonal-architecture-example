
namespace Common.Helper;

public sealed class MessageCreator
{

    //@SuppressWarnings("rawtypes")
    //public static Message<TPayload> Create<TPayload>(TPayload payload) where TPayload : AbstractMessage
    //{
    //    return MessageBuilder.withPayload(payload)
    //                         .setHeader(MessageHeaders.CONTENT_TYPE, MimeTypeUtils.APPLICATION_JSON)
    //                         .setHeader("correlationId", UUID.randomUUID().toString())
    //                         .setHeader("version", "1.2")
    //                         .setErrorChannelName("error_channel_" + payload.getName()) // just an ex
    //                         .setReplyChannelName("reply_channel_" + payload.getName())
    //                         .build();
    //}
}

